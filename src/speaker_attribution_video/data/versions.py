"""D1 schema versions for source, rights, and media manifests."""

from __future__ import annotations

DATA_ID_SCHEMA_VERSION = "d1.id.v1"
SOURCE_SCHEMA_VERSION = "d1.source.v1"
RIGHTS_SCHEMA_VERSION = "d1.rights.v1"
MANIFEST_SCHEMA_VERSION = "d1.manifest.v1"

SUPPORTED_DATA_ID_SCHEMA_VERSIONS = frozenset({DATA_ID_SCHEMA_VERSION})
SUPPORTED_SOURCE_SCHEMA_VERSIONS = frozenset({SOURCE_SCHEMA_VERSION})
SUPPORTED_RIGHTS_SCHEMA_VERSIONS = frozenset({RIGHTS_SCHEMA_VERSION})
SUPPORTED_MANIFEST_SCHEMA_VERSIONS = frozenset({MANIFEST_SCHEMA_VERSION})
