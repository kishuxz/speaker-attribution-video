Synthetic evidence-graph fixtures only.

Valid graphs use generic labels (`speaker_00`, `candidate_a`). They are engineering
tests, not accuracy benchmarks. No show names, real dialogue, or private model output.
